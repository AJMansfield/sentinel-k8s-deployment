apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "email-sender.fullname" . }}-test-connection"
  labels:
    {{- include "email-sender.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "email-sender.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
