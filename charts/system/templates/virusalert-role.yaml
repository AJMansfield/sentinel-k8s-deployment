apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-virusalert-role
  namespace: {{ .Release.Namespace }}
  labels: {{- include "system.labels" . | nindent 4 }}
stringData:
  roles.yml: |-
    virusalert_role:
      indices:
      - names: ["**"]
        privileges:
        - read